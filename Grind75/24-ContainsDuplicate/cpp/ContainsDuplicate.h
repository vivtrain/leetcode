#ifndef CONTAINS_DUPLICATE
#define CONTAINS_DUPLICATE

#include <vector>

class ContainsDuplicate {
  public:
    bool containsDuplicate(std::vector<int>& nums);
    bool containsDuplicate2(std::vector<int>& nums);
  private:
};

#endif

