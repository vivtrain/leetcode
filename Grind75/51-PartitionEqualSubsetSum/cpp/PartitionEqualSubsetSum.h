#ifndef PARTITION_EQUAL_SUBSET_SUM
#define PARTITION_EQUAL_SUBSET_SUM

#include <unordered_map>
#include <map>
#include <vector>

class Solution {
  public:
    bool canPartition(std::vector<int> &nums);
};

#endif

