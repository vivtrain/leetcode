#ifndef RANSOM_NOTE
#define RANSOM_NOTE

#include <string>

class RansomNote {
  public:
    bool canConstruct(std::string ransomNote, std::string magazine);
    bool canConstruct2(std::string ransomNote, std::string magazine);
    bool canConstruct3(std::string ransomNote, std::string magazine);
  private:
};

#endif

