#ifndef MERGE_K_SORTED_LISTS
#define MERGE_K_SORTED_LISTS

#include <List.h>
#include <vector>

class Solution {
  public:
    List::ListNode* mergeKLists(std::vector<List::ListNode*>& lists);
};

#endif

