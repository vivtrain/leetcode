#ifndef TWOSUM
#define TWOSUM

#include <vector>

class TwoSum {
  public:
    std::vector<int> twoSum_sort(std::vector<int>& nums, int target);
    std::vector<int> twoSum(std::vector<int>& nums, int target);
  private:
};

#endif

